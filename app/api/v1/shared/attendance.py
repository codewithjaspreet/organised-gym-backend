from fastapi import APIRouter

router = APIRouter(prefix="/attendance", tags=["attendance"])

# Attendance endpoints will be implemented here
# Currently empty as attendance.py was empty in the original structure

